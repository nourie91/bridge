{
  "name": "bridge-server",
  "version": "1.0.0",
  "description": "Local server connecting Claude Code to Figma Plugin API via WebSocket",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  }
}
