node_modules/
.DS_Store
**/.DS_Store
.tmp/
.claude/

# Internal design docs (specs, plans) — kept locally, not pushed.
docs/superpowers/
docs/plans/
docs/specs/

# Unused legacy asset.
bridge-logo.png

# TypeScript build output
dist/
*.tsbuildinfo

# Bridge Docs artifacts (generated by Bridge itself in user repos, not versioned here).
# Bridge is a CLI tool, not a real design system — it does not self-document.
docs.config.yaml
.bridge/
bridge-ds/
design-system/
llms.txt
